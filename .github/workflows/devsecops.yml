name: juice-shop-sonarqube
# Workflow solo SonarQube
on:
  push:
    branches: [ develop ]
jobs:
  sonarqube:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Run SonarQube
        run: echo "Ejecutando SonarQube..."
